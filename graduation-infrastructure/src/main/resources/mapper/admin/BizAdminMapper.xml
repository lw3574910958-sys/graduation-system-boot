<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lw.graduation.infrastructure.mapper.admin.BizAdminMapper">

    <!-- 继承通用Mapper的基础配置 -->
    <resultMap id="BaseResultMap" type="com.lw.graduation.domain.entity.admin.BizAdmin">
        <id column="id" property="id" />
        <result column="user_id" property="userId" />
        <result column="admin_id" property="adminId" />
        <result column="department_id" property="departmentId" />
        <result column="role_level" property="roleLevel" />
        <result column="phone" property="phone" />
        <result column="email" property="email" />
        <result column="created_at" property="createdAt" />
        <result column="updated_at" property="updatedAt" />
        <result column="is_deleted" property="isDeleted" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, user_id, admin_id, department_id, role_level, phone, email, created_at, updated_at, is_deleted
    </sql>
    
    <!-- 实现通用批量查询详情及关联信息 -->
    <select id="selectDetailsWithRelations" resultType="map">
        SELECT 
            a.id,
            a.user_id,
            a.admin_id,
            a.department_id,
            a.role_level,
            a.phone,
            a.email,
            a.created_at,
            a.updated_at,
            u.real_name as admin_name,
            d.name as department_name
        FROM biz_admin a
        LEFT JOIN sys_user u ON a.user_id = u.id
        LEFT JOIN sys_department d ON a.department_id = d.id
        WHERE a.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND a.is_deleted = 0
    </select>
    
    <!-- 实现增强版批量查询 -->
    <select id="selectBatchWithOrder" resultMap="BaseResultMap">
        SELECT 
        <include refid="Base_Column_List" />
        FROM biz_admin
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND is_deleted = 0
        ORDER BY FIELD(id, 
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </select>
    
    <!-- 实现通用统计方法 -->
    <select id="selectStatistics" resultType="map">
        SELECT 
            COUNT(*) as total_count,
            COUNT(CASE WHEN role_level = 1 THEN 1 END) as super_admin_count,
            COUNT(CASE WHEN role_level = 2 THEN 1 END) as admin_count
        FROM biz_admin
        WHERE is_deleted = 0
    </select>

</mapper>
