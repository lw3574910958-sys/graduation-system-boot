<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lw.graduation.infrastructure.mapper.MyBaseMapper">
    
    <!-- 
        ================================================
        通用Mapper基础模板文件
        ================================================
        此文件为模板文件，各具体Mapper应继承此类并提供具体实现
        
        注意事项：
        1. 这是一个模板文件，不包含实际的SQL实现
        2. 各子Mapper需要覆盖Base_Column_List和BaseResultMap
        3. 通用方法需要在各子Mapper中具体实现
        4. 请勿直接在此文件中执行SQL语句
    -->
    
    <!-- 通用查询结果列（各子Mapper需覆盖此定义） -->
    <sql id="Base_Column_List">
        <!-- 
            ⚠️ 重要：此为模板定义
            各具体Mapper XML文件中需要重新定义此片段
            示例：
            <sql id="Base_Column_List">
                id, name, created_at, updated_at, is_deleted
            </sql>
        -->
    </sql>
    
    <!-- 通用查询映射结果（各子Mapper需覆盖此定义） -->
    <resultMap id="BaseResultMap" type="java.lang.Object">
        <!-- 
            ⚠️ 重要：此为模板定义
            各具体Mapper XML文件中需要重新定义此映射
            示例：
            <resultMap id="BaseResultMap" type="com.example.YourEntity">
                <id column="id" property="id" />
                <result column="name" property="name" />
            </resultMap>
        -->
    </resultMap>
    
    <!-- ==================================================== -->
    <!-- 通用方法模板（各子Mapper需提供具体实现） -->
    <!-- ==================================================== -->
    
    <!-- 
        通用批量查询详情及关联信息
        各子Mapper需提供具体实现
    -->
    <!-- 
    <select id="selectDetailsWithRelations" resultType="map">
        SELECT 
            your_columns
        FROM your_table
        WHERE conditions
    </select>
    -->
    
    <!-- 
        通用批量查询（增强版）
        各子Mapper需提供具体实现
    -->
    <!-- 
    <select id="selectBatchWithOrder" resultMap="BaseResultMap">
        SELECT columns
        FROM table
        WHERE conditions
        ORDER BY FIELD(...)
    </select>
    -->
    
    <!-- 
        通用统计方法
        各子Mapper需提供具体实现
    -->
    <!-- 
    <select id="selectStatistics" resultType="map">
        SELECT statistics_query
        FROM table
        WHERE conditions
        GROUP BY ...
    </select>
    -->

</mapper>